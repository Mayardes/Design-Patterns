namespace S.O.L.I.D
{
    internal class Journal
    {
        private readonly List<string> _entries = new List<string>();

        //just a counter for total # entries.
        private static int count = 0;
    }
}
